# AGENTS.md

このリポジトリで作業する AI エージェント（Codex / Claude Code など）向けの共通ルールです。
Claude Code は `CLAUDE.md` からこのファイルを読み込みます。人間が読んでも構いません。

## このリポジトリは何か

営業日報アプリ（Google Apps Script + スプレッドシート「日報カウンター」）の入力内容を
集計し、翌営業日の朝に担当者へまとめメールを送るための自動化です。
仕様・データ構造の詳細は `README.md` を参照してください。

Web アプリでもライブラリでもありません。**ビルド・テスト・依存パッケージはありません。**
`npm install` や `pytest` などを探しても存在しないので、実行しようとしないでください。

## ディレクトリ構成

| パス | 内容 |
| --- | --- |
| `README.md` | 仕様・データ構造・実装方式の説明。仕様を変えたらここも直す |
| `gas/DailyReportMailer.gs` | まとめメール送信の Apps Script（本番運用の主系） |
| `gas/Receiver.gs` | 入力アプリからの送信を受けてスプレッドシートに書き込む受け皿 |
| `gas/README.md` | 上記2ファイルの導入手順 |
| `docs/routine-prompt.md` | Claude のルーティン（定期実行）に登録するプロンプト |
| `docs/step1-receiver-setup.md` | 受け皿の設置手順（利用者向け） |
| `docs/step2-connection-test.md` | 入力アプリ→受け皿の疎通テスト手順（利用者向け） |

## 絶対に守ること：社内情報をコミットしない

このリポジトリには、次のものを**値として書かないでください**。
プレースホルダ（`<SPREADSHEET_ID>` など）か、スクリプトプロパティ経由の参照にします。

- スプレッドシート ID、ファイル ID、ドライブの URL
- メールアドレス（宛先・CC・個人のもの全部）
- 個人名・会社名・取引先名
- 受け皿の合言葉（`TOKEN`）、ウェブアプリの `exec` URL
- 日報の実データ（数値・コメント本文）

実値は Apps Script の**スクリプトプロパティ**か、Claude のルーティン設定側で持ちます。
生成物のプレビュー HTML は `.gitignore` 済みです（`mail-preview.html` / `*.preview.html`）。

作業中に上記が混ざったファイルを見つけたら、**勝手に消さずに報告**してください。

## 書き方の決まり

- ドキュメント・コメント・コミットメッセージは**日本語**。
- 利用者向けドキュメント（`docs/step*.md`）は非エンジニア向け。専門用語には
  一言の説明を添え、手順は番号付きで書く既存のトーンに合わせる。
- `.gs` は Apps Script（V8）。`const`/`let`・アロー関数は使えるが、
  npm パッケージ・`import` は使えない。既存ファイルの関数分割とコメントの粒度に合わせる。
- 仕様に関わる変更をしたら `README.md` と該当する `docs/` を同じコミットで更新する。

## 検証のしかた

CI もテストもありません。変更したら以下で確認してください。

- `.gs` の構文チェック：`node --check` は `.gs` 拡張子を受け付けないので、
  一時的に `.js` としてコピーしてから確認します。

  ```sh
  cp gas/DailyReportMailer.gs /tmp/check.js && node --check /tmp/check.js
  ```

  （`SpreadsheetApp` などは Node に無いので、**実行はできません**。構文だけ確認します）
- メール文面の確認：Apps Script エディタ上で `previewDailyReportMail` を実行（送信はされない）
- 受け皿の確認：Apps Script エディタ上で `selfTest` を実行

実際の送信を伴う関数（`sendDailyReportMail` など）を、確認なしに実行させる手順を
書かないでください。

## Git の運用

- `claude/<内容>-<識別子>` 形式のブランチで作業し、そのブランチに push する。
- コミットメッセージは日本語一行＋必要なら本文。何をなぜ変えたかを書く。
- Pull Request は**依頼されたときだけ**作る。
- 外部への送信（メール送信、外部サービスへの投稿）を伴う操作は、実行前に必ず利用者に確認する。
