# CLAUDE.md

このリポジトリで Claude が作業するときのルール。

## このリポジトリについて

営業日報アプリ（Google Apps Script + スプレッドシート「日報カウンター」）の入力内容を、
翌営業日の朝にまとめてメール送信するための自動化。仕様の詳細は `README.md` を参照。

```
docs/routine-prompt.md      Claude ルーティン（平日 8:00 JST 起動）の設定とプロンプト
docs/step1-receiver-setup.md 受け皿ウェブアプリの導入手順
docs/step2-connection-test.md 入力アプリ→受け皿の疎通テスト手順
gas/DailyReportMailer.gs    まとめメール送信（Apps Script 版・恒久運用用）
gas/Receiver.gs             入力アプリからの POST を受けてスプレッドシートに書く受け皿
gas/README.md               DailyReportMailer の導入手順
```

## 絶対に守ること

### 1. 社内情報をリポジトリに書かない

次のものは**コミットしない**。ドキュメントに書くときは
`<SPREADSHEET_ID>` `<RECIPIENT>` のようなプレースホルダにする。

- スプレッドシートID、ドキュメントID、ウェブアプリの URL
- 宛先・CC のメールアドレス、社員名・担当者名
- `TOKEN` などの合言葉・認証情報
- 実データ（件数、コメント本文、顧客・物件に関する記述）

実値は実行環境側（Claude のルーティン設定、または Apps Script のスクリプトプロパティ）で管理する。
生成物のプレビュー HTML は `.gitignore` 済み。新しく社内情報を含むファイルを作る場合は
必ず `.gitignore` に追加してからコミットする。

### 2. 外部への送信・投稿は必ず事前確認

メール送信、外部サービスへの投稿、共有リンクの発行は、
**実行前に必ず内容を提示して確認を取る**。定期ルーティンとして既に承認済みの
まとめメール送信だけが例外で、それ以外は勝手に送らない。

### 3. 情報を勝手に消さない

ファイル・シート・ドキュメントの削除や上書きは、対象を確認したうえで、
必要性を説明してから行う。判断に迷ったら残す。

### 4. 数字を推測で埋めない

集計値はスプレッドシートの値をそのまま使う。読めなかった行や欠損は
「不明」として明示し、辻褄合わせをしない。正しく集計できないときは
メールを送らず、原因を報告して止まる。

## ドメイン上の注意

- **カウンターは週単位の累計値**であり、日別の増分ではない。集計は
  「対象日に入力（保存）があったレコードの、その時点の週累計値」になる。
  メール本文でもこの点を明記する。日別の値が必要なら入力アプリ側の変更が必要。
- **対象日の決め方**：火〜金の配信は前日1日分、月曜の配信は前週の金・土・日の3日分。
  土日は配信しない。祝日は考慮していない（配信され「入力なし」になる）。
- **cron は UTC**。JST 8:00 は UTC 23:00 で日付が前日に戻るため、
  曜日指定も1日ずらす（月〜金 → `0 23 * * 0-4`）。
- 対象日の入力が0件でも「入力はありませんでした」と明記して必ず送信する。

## 書き方の約束

- ドキュメント・コメント・コミットメッセージは日本語。
- 手順書は、Google の管理画面を触る人が迷わない粒度で書く
  （メニュー名をそのまま書く。`docs/step1-receiver-setup.md` の体裁に合わせる）。
- Apps Script は素の GAS（ライブラリ・ビルド手順なし）。貼り付けてそのまま動く状態を保つ。
- テストランナーやビルドは無し。GAS の変更は、まず `preview*` 系の関数で
  文面や結果を確認してから送信系を有効にする。

## 作業の進め方

- 作業ブランチは `claude/<内容>-<id>` 形式。`main` に直接コミットしない。
- 変更したら、何をどう変えたかと、利用者側で必要な操作（再デプロイ、
  スクリプトプロパティの追加など）をあわせて報告する。
