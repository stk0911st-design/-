# archive/ — 過去データの保管場所

使わなくなった入力データや、古いレポートをここに**移動**してください。

- `input/` から移動すると、AIの読み込み対象から外れます（実行が速くなります）。
- 削除ではなく移動なので、あとから見返せます。
- このシステムは archive/ の中身を自動で消しません。

```bash
# 例：8月の入力データを退避する
mv input/売上実績_2026-08.csv archive/
```
